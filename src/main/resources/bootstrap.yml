spring:
  application:
    name: gateway
  cloud:
    config:
      name: gateway
      discovery:
        service-id: config
        enabled: true

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}/eureka/